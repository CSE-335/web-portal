import { Button, Group } from "@mantine/core";

export default function UtilityNav() {
  return (
    <Group>
      <Button>Dark</Button>
      <Button>Login</Button>
      <Button>Sign Up</Button>
    </Group>
  );
}
